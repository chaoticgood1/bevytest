pub mod camera;
pub mod fly_camera;

// use bevy::prelude::*;

// pub struct CustomPlugin;
// impl Plugin for CustomPlugin {
//   fn build(&self, app: &mut AppBuilder) {
//     app.add_plugin(camera::CustomPlugin);
//   }
// }
